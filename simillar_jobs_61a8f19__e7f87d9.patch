diff --git a/job/templates/job-details.html b/job/templates/job-details.html
index e70f6d0..563b3c6 100644
--- a/job/templates/job-details.html
+++ b/job/templates/job-details.html
@@ -212,6 +212,7 @@
 
 
     function  loadJob(data){
+        console.log(data);
         container = $('.job-detail');
         if(data){
             $('.save').attr("href", data.job_id);
@@ -305,6 +306,19 @@
     function loadSimilarJob(data) {
         if(data.length > 0){
             for(index=0;index<6;index++){
+                var dateMomentString = 'None'
+                 var deadline= data[index].application_deadline;
+            var momentObj = moment(data[index].post_date, 'YYYY-MM-DD');
+            data[index].post_date = momentObj.format('DD/MM/YYYY');
+            // var created_date = data.created_at
+            // data.created_at = created_date.split("-").reverse().join("-")
+            if(data[index].application_deadline){
+                var app_deadline = moment(data[index].application_deadline, 'YYYY-MM-DD');
+                data[index].application_deadline = app_deadline.format('DD/MM/YYYY');
+            }
+            else{
+                 data[index].application_deadline = dateMomentString;
+            }
             var job_html= '<div class="job-list">'+
                 '<div class="thumb">'+
                 '<a href="#">'+
@@ -315,9 +329,9 @@
                 '<div class="content">'+
                 '<h4><a href="/job-detail/'+ data[index].slug+'">'+data[index].title+'</a></h4>'+
                 '<div class="info">'+
-                '<span class="company"><a href="#"><i class="fas fa-building" style="margin-right: 5px"></i>'+data[index].company_name+'</a></span>'+
-                '<span class="office-location"><a href="#"><i data-feather="map-pin"></i>'+data[index].job_area+'</a></span>'+
-                '<span class="job-type full-time"><a href="#"><i data-feather="clock"></i>'+data[index].job_type+'</a></span>'+
+                '<span class="company"><a><i class="fas fa-building" style="margin-right: 5px"></i>'+data[index].company_name+'</a></span>'+
+                '<span class="office-location"><a><i data-feather="map-pin"></i>'+data[index].job_area+'</a></span>'+
+                '<span class="job-type full-time"><a><i data-feather="clock"></i><span class="text-normal">'+data[index].job_type+'</span></a></span>'+
                 '</div>'+
                 '</div>'+
                 '<div class="more">'+
diff --git a/templates/home.html b/templates/home.html
index 39975b8..0105e69 100644
--- a/templates/home.html
+++ b/templates/home.html
@@ -370,7 +370,7 @@
                 if(data[index].application_deadline){
                     var dateString = data[index].application_deadline;
                     var momentObj = moment(dateString, 'YYYY-MM-DD');
-                    dateMomentString = momentObj.format('MMM DD, YYYY');
+                    dateMomentString = momentObj.format('DD/MM/YYYY');
                 }
                 var image_url;
                 if (data[index].company.profile_picture){
